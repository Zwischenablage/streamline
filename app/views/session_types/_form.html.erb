<%= form_with(model: session_type, local: true) do |form| %>
  <% if session_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(session_type.errors.count, "error") %> prohibited this session_type from being saved:</h2>

      <ul>
      <% session_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
