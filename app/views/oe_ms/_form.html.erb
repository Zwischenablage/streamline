<%= form_with(model: oem, local: true) do |form| %>
  <% if oem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(oem.errors.count, "error") %> prohibited this oem from being saved:</h2>

      <ul>
      <% oem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
