<%= simple_form_for [@customer, @project] do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :oracleID %>
  <%= f.button :submit, class: "btn btn-primary btn-sm" %>
<% end %>
